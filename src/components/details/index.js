import { Outlet } from "react-router";

const Details = () => {
    return (
        <div>
            <h1>Queen Zone Alert</h1>
            <Outlet />
        </div>
    );
};

export default Details;
